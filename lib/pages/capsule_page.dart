import 'package:flutter/material.dart';

class CapsulePage extends StatelessWidget {
  const CapsulePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("CapsulePage")),
    );
  }
}